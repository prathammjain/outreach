# ── PhonePe Configuration ──────────────────────────────────────────────────────
# Get these from PhonePe merchant dashboard → Developer Settings
PHONEPE_CLIENT_ID=your_client_id_here
PHONEPE_CLIENT_SECRET=your_client_secret_here
PHONEPE_CLIENT_VERSION=1
PHONEPE_ENV=SANDBOX   # Change to PRODUCTION when live

# Set these in PhonePe dashboard → Developer Settings → Webhook (S2S Callback)
PHONEPE_CALLBACK_USERNAME=your_callback_username
PHONEPE_CALLBACK_PASSWORD=your_callback_password

# ── Backend public URL (used to build the redirect URL sent to PhonePe) ────────
BACKEND_URL=https://your-backend.railway.app

# ── Google Service Account ─────────────────────────────────────────────────────
# Option A – local dev: path to JSON key file
GOOGLE_SERVICE_ACCOUNT_FILE=./service-account.json

# Option B – production: base64-encoded JSON (run: base64 -i service-account.json)
# GOOGLE_SERVICE_ACCOUNT_JSON_BASE64=

# ── Database ───────────────────────────────────────────────────────────────────
# /data is the mounted Railway volume; use sqlite:///./payments.db for local dev
DATABASE_URL=sqlite:////data/payments.db

# ── Product Pricing (in paise) ────────────────────────────────────────────────
TIER_1_PRICE=99900    # ₹999
TIER_2_PRICE=149900   # ₹1,499

# ── Google Sheet IDs ───────────────────────────────────────────────────────────
INDIAN_SHEET_ID=your_indian_sheet_id_here
YC_SHEET_ID=your_yc_sheet_id_here

# ── Admin / Security ───────────────────────────────────────────────────────────
# Any strong random string; sent as X-Admin-Key header when calling /admin/revoke
ADMIN_API_KEY=generate_a_strong_random_key_here

# Comma-separated list of your frontend domain(s) for CORS
# Leave empty to allow all origins (dev only)
ALLOWED_ORIGINS=https://your-frontend-domain.com
